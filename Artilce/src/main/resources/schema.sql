create table article(aid int NOT NULL AUTO_INCREMENT,username varchar(100),title varchar(100),type varchar(100),
content longtext(2000),image text(500),date TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,primary key(aid));