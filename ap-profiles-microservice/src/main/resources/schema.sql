create table users(id int primary key, username varchar (50), name varchar(100), email varchar(100), password varchar(100),number int);

create table admin(id int primary key, username varchar (50), name varchar(100), email varchar(100), password varchar(100),number int);
